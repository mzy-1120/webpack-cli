// const array = [1, 2, 3, 4, 5];
// const filteredArray = $.grep(array, function (item) {
//   return item > 2; // 过滤条件
// });

// import array from "@mzy1120/utils-array";

import { add } from "lodash";

const a = Math.random();
const b = Math.random();
const c = Math.random();

// const newPerson = array.deepCopy(person);
// newPerson.hobbies.push("gaming");

console.log("person", add(a, b, c)); // 输出: [3, 4, 5];
